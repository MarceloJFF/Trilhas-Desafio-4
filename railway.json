{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacks": {
      "buildCommand": "chmod +x ./mvnw && ./mvnw clean package "
    }
  },
  "deploy": {
    "startCommand": "java -jar target/quarkus-run.jar"
  }
}
